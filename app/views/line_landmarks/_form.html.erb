<%= form_with(model: line_landmark, local: true) do |form| %>
  <% if line_landmark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(line_landmark.errors.count, "error") %> prohibited this line_landmark from being saved:</h2>

      <ul>
        <% line_landmark.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :landmark_id %>
    <%= form.text_field :landmark_id %>
  </div>

  <div class="field">
    <%= form.label :list_id %>
    <%= form.text_field :list_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
